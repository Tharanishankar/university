module.exports = {
  currency:         'USD',
  currencyRate:     1.0,
  diasporaCountries: [],
  // International student tuition fee range in USD
  // minUSD = state university minimum for international students
  // maxUSD = private university p90 (Ivy League excluded as outliers)
  // TO UPDATE: check College Board published data annually
  minUSD: 12000,
  maxUSD: 55000,
};
