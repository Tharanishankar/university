module.exports = {
  currency:         'AUD',
  currencyRate:     0.65,      // 1 AUD = $0.65 USD
  diasporaCountries: [],
  // International student tuition fee range in USD
  // minUSD: typical p10 for international students
  // maxUSD: typical p90 (not absolute max — outliers excluded)
  // TO UPDATE: check ARC/TEQSA published fee data annually
  minUSD: 18000,
  maxUSD: 36000,
};
